import { Stack, styled } from "@mui/material";

export const VideoStack = styled(Stack)(() => ({
  width: "100%",
  height: "100%",
}));

export const ControlAreaStack = styled(Stack)(() => ({
  width: "100%",
  height: "100%",
}));
